/**
 * Review
 **/

// Write a function `nameThatAnimal` that takes two arguments, a name, 
// and a type of animal.

	function nameThatAnimal (name, animal) {
							// VARIABLES

// The function should alert a sentence naming the animal.
	alert('The ' + animal + ' is called ' + name + '.');
	
	}
	
// Call your function with a funny name and a real animal.

	nameThatAnimal('Missy','cat');
					// our values